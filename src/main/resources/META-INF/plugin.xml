<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
  <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
  <id>net.nightvision.plugin</id>

  <!-- Public plugin name should be written in Title Case.
       Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
  <name>Nightvision</name>

  <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
  <vendor email="support@nightviz.ai" url="https://www.nightviz.ai/">NightVision</vendor>

  <!-- The description shown on the Plugin Page and in the IDE Plugin Manager is
       injected at build time by patchPluginXml from the "Plugin description"
       section of README.md (see build.gradle.kts). Do not add one here. -->

  <!-- The change-notes shown on the Plugin Page and in the IDE Plugin Manager are
       injected at build time by patchPluginXml from CHANGELOG.md (see the changelog
       block in build.gradle.kts). Do not add them here. -->

  <!-- Product and plugin compatibility requirements.
       Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
  <depends>com.intellij.modules.platform</depends>

  <!-- Extension points defined by the plugin.
       Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
  <extensions defaultExtensionNs="com.intellij">
    <toolWindow id="NightVision" secondary="true" icon="/icons/nightvision-logo.svg" anchor="right"
                factoryClass="net.nightvision.plugin.MainWindowFactory" />
  </extensions>

  <!-- It is an interesting feature, maybe we can consider using these actions, so leaving it here for reference... -->
  <!-- <actions>
    <group></group>
    <action id="scan-repository" class="net.nightvision.plugin.actions.ScanRepositoryAction" text="(NV) Scan Repository">
      <add-to-group group-id="ToolsMenu" anchor="first"></add-to-group>
    </action>
  </actions> -->
</idea-plugin>